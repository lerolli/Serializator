package FileWorker;

import ThreadDispather.ThreadedTask;

import java.net.Socket;

public class CommandFactory {

    public static ICommand createInstance(ThreadedTask task, Socket s) {
        return null;
    }
}
